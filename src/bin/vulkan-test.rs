
use std::time::Duration;

fn main() {

    // Initialize Vulkan

    // Mainloop
    'running: loop {
        
        break 'running
        
        ::std::thread::sleep(Duration::new(0, 1_000_000_000u32 / 480));
    }
    // Cleanup
}